import SwiftUI

struct MoreView: View {
    var body: some View {
        VStack {
            Image(systemName: "globe")
            Text("Hello World!")
        }
    }
}

#Preview {
    MainTabView(selection: 3)
}
